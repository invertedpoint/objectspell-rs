[package]
name = "pubsub"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
objectspell = { path = "../../objectspell" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
serde_json = "1"
async-trait = "0.1"
